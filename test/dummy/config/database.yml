default: &default
  adapter: sqlite3
  timeout: 5000
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  database: ":memory:"

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
