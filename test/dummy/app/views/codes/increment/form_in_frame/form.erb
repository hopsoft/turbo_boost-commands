<%= form_with url: "path/to/turbo-frame", method: :patch,
  data: {
    turbo_reflex: "IncrementReflex#increment",
    count: @count.to_i
  } do |form| %>
  <%= form.submit "Submit Form" %>
<% end %>
