<%= tag.div class: "animate-ping inline-flex absolute -top-2 -right-2 justify-center items-center w-4 h-4 text-xs font-bold text-white bg-red-500 rounded-full border-2 border-white dark:border-gray-900 #{local_assigns[:class]}" do %>
  <small><%= value %></small>
<% end %>
