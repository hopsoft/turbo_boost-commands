<%= tag.details id: id,
  open: turbo_boost.command_state.dig(id, :attributes, :open),
  data: {
    controller: "state",
    action: "toggle->state#saveAttributes",
    state_attributes_value: ["open", "title"].to_json,
    tooltip: turbo_boost.command_state.dig(id, :attributes, "data-tooltip")
  } do %>
  <%= yield %>
<% end %>
